'''keys: Solutions:Similar:T:S:'''from typing import List# https://leetcode.com/problems/non-overlapping-intervals/discuss/276056/Python-Greedy-Interval-Scheduling'''always pick the interval with the earliest end time. Then you can get the maximal number of non-overlapping intervals. (or minimal number to remove).'''class Solution:    # T: O(NlogN)    # S: O(1), based on end point sorting    def eraseOverlapIntervals(self, intervals: List[List[int]]) -> int:        if not intervals: return 0        prev_end, ct = float("-inf"), 0        # you can also sort by (x[1], x[0]), but cost more time        # e.g., [[1,100],[11,22],[1,11],[2,12]]        # if you only sort by x[0], then you'll have [[1,100], [1,11]]        intervals.sort(key = lambda x: x[1])        for s, e in intervals:            if s >= prev_end:                prev_end = e            else: # s < pre_end, overlap, then remove the current interval                ct += 1        return ct    # based on start point sorting    def eraseOverlapIntervals1(self, intervals: List[List[int]]) -> int:        if not intervals: return 0        intervals.sort(key = lambda x: x[1])